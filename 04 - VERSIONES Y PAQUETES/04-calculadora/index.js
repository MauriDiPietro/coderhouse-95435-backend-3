const operacionSumar = (a,b) => a + b;
const operacionRestar = (a,b) => a - b;
const operacionMultiplicar = (a,b) => a * b;
const operacionDividir = (a,b) => a / b;

module.exports = {
    operacionSumar,
    operacionRestar,
    operacionMultiplicar,
    operacionDividir
}